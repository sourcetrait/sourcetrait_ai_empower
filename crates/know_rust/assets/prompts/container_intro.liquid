# Orientation: Container Workspace

The structural signals indicate this workspace is a container - a sub-topic aggregator with no single architectural pattern. Each member is a separate topical library; running the picker across the workspace as a whole would produce confidently-wrong output. This artifact routes you to the per-member orientations instead.

```
{{ provenance }}
```
