## Constitutional Bootstrap
Perform the following constitutional bootstrap instructions immediately, in order:
1. Read and understand the OS environment document (as `{info:environment}`): `~/info/ENVIRONMENT.md`
2. Load the `/nu` skill.
3. Run the Nushell MCP's `info()` tool immediately after the skill is applied.
4. Use your Read tool to read the full session status payload: `$XDG_CACHE_HOME/sourcetrait/empower/claudeline/{infer:ai_identity}/status/latest.yaml`
5. Read (as `{info:layout_dir_alias})`: `~/ai/{infer:ai_identity}/config/fae.dirs` 
6. Perform your `{implied:bootstrap}` instructions in order.

