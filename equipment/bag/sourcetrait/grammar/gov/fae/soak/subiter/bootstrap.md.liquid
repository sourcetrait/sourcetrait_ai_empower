# subiter: {{ iter }}:{{ subiter }}: bootstrap.md
> USER

## bootstrap: {{ iter }}:{{ subiter }}
Perform the following iter bootstrap instructions, in order:
1. Perform `{protocol:iter_bootstrap}` for the parent iter: `./iter/{{ iter }}
1. Perform `{protocol:iter_working_bootstrap}` for this subiter: `./iter/{{ iter }}/subiter/{{ subiter }}/understood`
2. Upon completion, await instruction.

## subiter: {{ iter }}:{{ subiter }}
- `config.yaml` Configuration data points
- `debt.md` Rolling technical debt
- `design.md` Authorative user design intentions
- `followup.md` Rolling planned followup tasks
- `journal.md` Historical meta-data
- `soak.yaml` Soak representation of this Iter
- `operation.md` Rolling operational information (how-to)
- `historical/` Historical `findings/`, `resolved/` tasks, and retired `journals/` 
- `operational/` Operational resources as described by `operation.md`
- `understood/` Rolling understanding of topic
