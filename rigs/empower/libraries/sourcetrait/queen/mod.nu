# Queen role library (sourcetrait/queen): the ant colony's leader.
#
# The bonded-fae comms channel lives under `channel`; queen-invoked management
# of a drone's channel under `drone` (see sourcetrait/queen:drone/channel);
# shared derivations in `common` (wrapping sourcetrait/ant).
export module channel
export module drone
export module common
