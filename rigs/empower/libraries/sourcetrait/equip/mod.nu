# Equip core library (sourcetrait/equip).
#
# The ambient operator-config surface an equipped agent reads: its store
# coordinate (id + namespace) and its work dir, each with a user-shell
# fallback. See equip:session.
export module session
