## bootstrap: authorative 
Perform the following authorative bootstrap instructions immediately, in order:
1. Read and understand your configuration: `./config/fae.yaml`
   - Understand config `layout` as `{info:layout}`.
2. Read and understand the OS environment document (as `{info:environment}`): `~/info/ENVIRONMENT.md`
3. Load the `/nu` skill.
4. Run the Nushell MCP's `info()` tool immediately after the skill is applied.
5. Use your Read tool to read the full session status payload: `$XDG_CACHE_HOME/sourcetrait/empower/claudeline/{infer:ai_identity}/status/latest.yaml`
6. Perform your `{implied:bootstrap}` instructions in order.

