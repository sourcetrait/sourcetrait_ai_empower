# Drone role library (sourcetrait/drone): a queen-launched colony teammate.
#
# The drone-invoked side of its bonded-fae channel lives under `channel`
# (ready/done/syn/ack); the queen-invoked open/close live in
# sourcetrait/queen:drone/channel. Shared derivations in `common` (wrapping
# sourcetrait/ant).
export module channel
export module common
