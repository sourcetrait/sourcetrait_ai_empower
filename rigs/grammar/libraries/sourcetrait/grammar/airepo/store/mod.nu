# The store: the fae's durable home for bulk that must cross systems.
#
# Reached from the ai repository through a gitignored `store` symlink, so no
# system-specific path is ever committed. `dev` stays empty and work rides the
# highest `rev/rev<N>` branch. Concerns nest beneath: grammar:airepo/store/git.
export module git
