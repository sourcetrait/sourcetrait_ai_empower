Workspace is a pure **end-user product** (end_use). Architecture / public / inter-crate / clique sets are the user-facing entry points and the cross-crate flows that compose the product's behavior. No external library face to track. Note: this bucket has no empirical anchor in the current 10-target reference set; guidance is speculative.
