## 7. Pattern-authoring guides

**[AGENT]** From the trait / type definitions in S2 and the significance sets in S5, write the minimal checklist to author a NEW instance of one of the Tier 1-3 patterns (S5.1 architecture, S5.2 public, S5.3 inter-crate, or S5.4 clique). {{ classification_tag }}Frame the checklist for the consumership the workspace serves: dev_use workspaces author against the library's public API; end_with_dev_use workspaces author internal-product features whose cross-crate flow matters; dev_with_end_use treats the lib as primary; end_use authors user-facing entry points. Anchor each step to a span. Apply the S5 authoring guidance per item: form-vs-role + per-category decomposition + pass discipline + reduction-through-inference.
