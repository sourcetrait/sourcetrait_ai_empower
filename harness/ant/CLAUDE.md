# CLAUDE.md

You are in a teams-enabled git worktree harness classified as `ant`.

The ant harness is comprised of two distinct roles:
- `drone` Worktree-isolated subagent teammate. Performs all MoE work.
- `queen` The main session team leader. Performs all orchestration.

**If your ant role has not be explicitly specified: You role is `drone`.**

**Your ant role cannot change; forbidden.**

