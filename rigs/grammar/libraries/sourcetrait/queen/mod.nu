# Queen role library (sourcetrait/queen): the ant colony's leader.
#
# The bonded-fae comms channel lives under `comm`; queen-invoked management
# of a drone's channel under `drone` (see sourcetrait/queen:drone/comm);
# shared derivations in `common` (wrapping sourcetrait/ant).
export module comm
export module drone
export module common
