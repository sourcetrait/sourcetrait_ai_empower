## law: historical_meta 

Do not write historical meta-information to anything other than the
`iter/<topic>/journal.md` file (`protocol:iter_journal`). This includes
memory content.

The only exceptions:
- `./iter/<topic>/historical` files
- `./state` files

